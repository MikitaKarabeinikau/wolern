from wolern.src.vocabulary import add_word_to_vocabulary, vocab, show_vocabulary_list, get_vocabulary, \
    show_all_vocabularies
from wolern.src.word import Word

if __name__ == '__main__':
    # print(show_vocabulary_list(get_vocabulary()))
    show_all_vocabularies()